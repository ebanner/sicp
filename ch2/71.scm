; EXERCISE 2.71

; Since the sum of the lightest two trees are going to be juuuuuuuust
; less (1 short) of the weight of the next node, the tree is built
; ``sequentially" (Thx Eli Bendersky for this term).
;
; The bits needed to encode the most frequently occurring symbol in
; such a bizarre frequency map is precisely n-1.
;
; The bits needed to encode the least frequently occurring symbol in
; such a bizarre frequency map is precisely n-1.
